# AGENTS.md — Agentic Workspace

> Activation rules for Gemini Code Assist, Jules, and Antigravity agents.
> Auto-generated by `generate_agent_rules.py` — edit manifest.json to update triggers.

## Core Instruction

Before responding to or executing any task, you MUST:

1. **Check the skills catalog**: Read `.agents/manifest.json` to identify which skill applies to the current context, using the `triggers` field of each skill as activation keywords.
2. **Load the skill**: Once a skill is identified, read its full `SKILL.md` and adopt that role, responsibilities, and guidelines.
3. **Load references**: Follow the `## Reference Loading` section in the skill to determine which reference files to read.
4. **Multi-skill tasks**: If a task spans multiple skills, activate them **sequentially in logical order** as defined in the `pipelines` section of `.agents/config.yaml`.

## Configuration

Read `.agents/config.yaml` for:
- Response language (`response_language`)
- Supported language stacks
- Per-skill behavior overrides
- Available multi-skill pipelines

## Available Skills

| Skill | Primary Triggers | Description |
|-------|-----------------|-------------|
| `software-architect` | "diseñar sistema", "arquitectura", "diagrama" (+4 more) | Diseña arquitectura de software. Genera diagramas C4, contra... |
| `structure-analyzer` | "analizar estructura", "mapa de dependencias", "analiza este proyecto" (+3 more) | Analiza estructura de proyectos existentes. Mapea dependenci... |
| `code-improver` | "mejorar código", "refactorizar", "code review" (+4 more) | Detecta code smells y propone refactoring. Aplica principios... |
| `project-scaffolder` | "crear proyecto", "inicializar", "setup" (+4 more) | Genera scaffolding de proyectos con estructura, configuració... |
| `test-strategist` | "escribir tests", "estrategia de testing", "cobertura" (+4 more) | Define estrategia de testing, detecta anti-patrones y guía l... |
| `security-auditor` | "auditar seguridad", "vulnerabilidades", "OWASP" (+4 more) | Audita seguridad del código. Revisa OWASP Top 10, manejo de ... |
| `docs-generator` | "generar documentación", "documentar", "API docs" (+4 more) | Genera documentación técnica: API docs, runbooks, guías de u... |
| `devops-engineer` | "devops engineer" | Diseña pipelines CI/CD, infraestructura como código (IaC), c... |
| `git-workflow` | "git workflow" | Define estrategias de branching, conventional commits, PR te... |
| `api-designer` | "api designer" | Diseña contratos de API (OpenAPI, GraphQL schemas), estrateg... |
| `performance-engineer` | "performance engineer" | Profiling, load testing, benchmarking, caching, APM.... |
| `database-designer` | "database designer" | ER diagrams, migration strategies, query optimization.... |

## Multi-Skill Pipelines

- **new-project**: Full pipeline for bootstrapping a new project
  - Steps: `software-architect` → `project-scaffolder` → `test-strategist` → `security-auditor` → `docs-generator`
- **code-review**: Comprehensive code quality and security review
  - Steps: `structure-analyzer` → `code-improver` → `security-auditor`
- **audit-full**: Full project health audit
  - Steps: `structure-analyzer` → `security-auditor` → `test-strategist` → `code-improver` → `docs-generator`
- **quick-review**: Fast review of a single file or small module
  - Steps: `code-improver` → `security-auditor`

## Response Language

All responses must be in the language specified in `.agents/config.yaml` → `response_language` (default: **Spanish**).

## Autonomous Operation (`/goal` command)

When the `/goal` command is used:
- Operate autonomously without pausing for confirmations
- Self-correct: run tests, read errors, fix, repeat until passing
- Use subagents for parallel workstreams
- Follow the skill pipeline defined in `.agents/config.yaml`
