# ============================================
# SynapSeed — Variables de entorno (ejemplo)
# ============================================
# Copiar este archivo a `.env` y rellenar los valores reales:
#   cp .env.example .env
# NUNCA commitear el archivo `.env` real al repositorio.

# -----------------------------------------------------------
# Entorno
# -----------------------------------------------------------
APP_ENV=development
DEBUG=true
LOG_LEVEL=INFO

# -----------------------------------------------------------
# Base de datos PostgreSQL + pgvector
# (los valores deben coincidir con los de docker-compose.yml)
# -----------------------------------------------------------
POSTGRES_USER=synapseed
POSTGRES_PASSWORD=synapseed
POSTGRES_DB=synapseed
POSTGRES_PORT=5432
POSTGRES_HOST=localhost
# URL que usa SQLAlchemy (async) en el backend
DATABASE_URL=postgresql+asyncpg://synapseed:synapseed@localhost:5432/synapseed
# URL síncrona para Alembic (usa psycopg2)
DATABASE_URL_SYNC=postgresql+psycopg2://synapseed:synapseed@localhost:5432/synapseed

# -----------------------------------------------------------
# Redis (broker de Celery, cache y pub/sub para SSE)
# -----------------------------------------------------------
REDIS_PORT=6379
REDIS_HOST=localhost
REDIS_URL=redis://localhost:6379/0
CELERY_BROKER_URL=redis://localhost:6379/1
CELERY_RESULT_BACKEND=redis://localhost:6379/2

# -----------------------------------------------------------
# Backend (FastAPI)
# -----------------------------------------------------------
BACKEND_PORT=8000
BACKEND_HOST=0.0.0.0
API_V1_PREFIX=/api/v1
PROJECT_NAME=SynapSeed

# -----------------------------------------------------------
# Frontend (Vite)
# -----------------------------------------------------------
FRONTEND_PORT=5173
VITE_API_URL=http://localhost:8000/api/v1

# -----------------------------------------------------------
# Seguridad / JWT
# -----------------------------------------------------------
# IMPORTANTE: generar un valor aleatorio seguro en producción
#   python -c "import secrets; print(secrets.token_urlsafe(64))"
JWT_SECRET=change-me-in-production-please-use-a-long-random-string
JWT_ALGORITHM=HS256
JWT_EXPIRE_HOURS=24

# CORS: orígenes permitidos para el frontend
BACKEND_CORS_ORIGINS=["http://localhost:5173","http://localhost:3000"]

# -----------------------------------------------------------
# Google Gemini (LLM y embeddings)
# -----------------------------------------------------------
# Obtener la API key en: https://aistudio.google.com/apikey
GEMINI_API_KEY=your-gemini-api-key-here
GEMINI_MODEL=gemini-2.0-flash
GEMINI_EMBEDDING_MODEL=text-embedding-004
GEMINI_EMBEDDING_DIM=768
# Rate limit (free tier Gemini 1.5 Flash ≈ 15 RPM)
GEMINI_RPM_LIMIT=15
GEMINI_MAX_RETRIES=5

# -----------------------------------------------------------
# Celery
# -----------------------------------------------------------
CELERY_TASK_TIME_LIMIT=600
CELERY_WORKER_PREFETCH_MULTIPLIER=1
CELERY_WORKER_CONCURRENCY=1

# -----------------------------------------------------------
# SSE (Server-Sent Events) para progreso del pipeline
# -----------------------------------------------------------
SSE_KEEPALIVE_INTERVAL=15
SSE_HEARTBEAT_TIMEOUT=120

# -----------------------------------------------------------
# Observabilidad
# -----------------------------------------------------------
SENTRY_DSN=
